---
inputSchema: !schema
  name: Input Schema
  columns:
    - name: Name
      type: string
    - name: Count
      type: integer
    - name: Price
      type: decimal

input1: !pipe
input2: !pipe
funnelOutput: !pipe
funnel: !funnel
  name: Test Funnel
  inputs:
    - ->input1
    - ->input2
  output: ->funnelOutput
